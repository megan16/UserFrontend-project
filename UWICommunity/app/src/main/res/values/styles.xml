<resources>


    <style name="myHeadingStyle">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">18sp</item>
        <item name="android:background">@color/dashboardDiv</item>
        <item name="android:textColor">@color/dashText</item>
        <item name="android:padding">10dp</item>
        <item name="android:typeface">sans</item>
    </style>



    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/upload</item>
    </style>

    <!-- Action Bar for Safety -->
    <style name="SafetyTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/SafetyBar</item>
    </style>

    <style name="SafetyBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:background">@color/safetyColor</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <!-- End of Action bar for safety -->


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
